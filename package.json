{
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "description": "Phased personal portfolio site (Phase 1: static, nginx + ngrok in Docker)",
  "type": "module",
  "engines": {
    "node": ">=18.14"
  },
  "scripts": {
    "fetch": "node scripts/fetch-github.mjs",
    "serve": "node scripts/dev-server.mjs"
  }
}
